# 지음 프로젝트 — 배포 안내서 (v7 확장)

작성일: 2026-08-04

---

## 1. 무엇이 바뀌었나 — 한눈에

| # | 항목 | 방식 | 결과 |
|---|---|---|---|
| ⑤ | 파일 분할 | `13-writing-games.js` → 13a~13f | 249KB 통짜 → 최대 70KB 6조각 |
| ④ | 약점 기반 추천 | `13b`·`13d` 수정 | 랜덤 추천 → 최근 덜 쓴 표현 기반 |
| ① | 단원 모드 | `14-curriculum.js` 신규 | 성취기준으로 활동·미션 필터링 |
| ③ | 계획하기 | `15-writing-plan.js` 신규 | 생각그물 30초 + 저장 후 대조 |
| ② | 자동 백업 유도 | `16-backup-nudge.js` 신규 | 5편마다·14일마다·휘발성 감지 시 권유 |

**기존 파일 중 수정된 것은 `index.html` 하나뿐입니다.** `01~12` 번 파일은 한 글자도 건드리지 않았습니다.

---

## 2. 업로드 방법

### 2-1. 올릴 파일

```
/js/13a-games-core.js      ← 신규 (분할본)
/js/13b-games-dodum.js     ← 신규 (분할본)
/js/13c-games-ieum.js      ← 신규 (분할본)
/js/13d-world-hub.js       ← 신규 (분할본)
/js/13e-world-art.js       ← 신규 (분할본)
/js/13f-games-init.js      ← 신규 (분할본)
/js/14-curriculum.js       ← 신규
/js/15-writing-plan.js     ← 신규
/js/16-backup-nudge.js     ← 신규
/index.html                ← 덮어쓰기
```

### 2-2. 구 파일 처리

`/js/13-writing-games.js` 는 **더 이상 로드되지 않습니다.**
바로 지워도 되고, 롤백 대비로 며칠 남겨 둬도 무방합니다 (로드되지 않으므로 성능 영향 없음).

### 2-3. 롤백 방법

문제가 생기면 `index.html` 의 스크립트 블록을 아래 한 줄로 되돌리면 즉시 원상 복구됩니다.

```html
<script src="js/13-writing-games.js?v=6"></script>
```

단, 이 경우 14·15·16 번 모듈도 함께 빼야 합니다 (13번 계열의 `wg*` 유틸을 쓰기 때문).

---

## 3. ⚠️ 반드시 지켜야 할 로드 규칙

`13a` ~ `13f` 는 **원래 하나의 IIFE 였던 코드를 펼친 것**이라 **여섯 개가 하나의 스코프를 공유**합니다.

- ✅ 반드시 **13a → 13b → 13c → 13d → 13e → 13f 순서로, 빠짐없이** 로드
- ❌ 하나라도 빠지면 `ReferenceError` 로 **게임 기능 전체가 멈춥니다**
- ❌ `type="module"` 이나 `defer` / `async` 를 붙이면 안 됩니다 (스코프 공유가 깨짐)
- ✅ 새 최상위 변수·함수를 추가할 땐 반드시 `wg` / `WG_` / `_wg` 접두어 사용 (전역 충돌 방지)

`index.html` 에 이미 이 경고를 주석으로 넣어 두었습니다.

---

## 4. 📖 단원표 채우기 — 선생님이 직접 하셔야 하는 부분

### 왜 비워 두었나

2022 개정 5~6학년 국어 교과서는 **2026학년도부터 처음 적용**되었습니다.
개발 시점에 실제 단원명을 확인할 수 없었고, **지어낸 단원명이 교실에서 그대로 쓰이는 것이 가장 나쁜 결과**라고 판단해 `WG_UNITS` 를 의도적으로 빈 배열로 두었습니다.

> 참고: 검색 결과로는 초등 국어가 국정 교과서로 유지된다는 내용이 있었습니다.
> 사실이라면 단원명이 전국 공통이므로 **한 번만 채우면 모든 학교에서 재사용** 가능합니다.
> 다만 이는 정책 브리핑 인터뷰 한 건에 근거한 것이므로 **실물 교과서로 확인**해 주세요.

### 채우지 않아도 동작합니다

단원표가 비어 있으면 앱이 자동으로 **「성취기준 영역」 선택**(듣말 / 읽기 / 쓰기 / 문법 / 문학 / 매체)으로 대체합니다. 이것만으로도 활동 필터링은 정상 작동합니다.

### 방법 A — 파일 직접 편집 (권장 · 영구 반영 · 모든 기기 공통)

`js/14-curriculum.js` 의 아래 줄을 찾습니다.

```js
const WG_UNITS = [];
```

교과서를 보고 이렇게 채웁니다.

```js
const WG_UNITS = [
  { grade: '5-1', no: 1, name: '교과서에 적힌 단원명', std: ['6국05-02'] },
  { grade: '5-1', no: 2, name: '교과서에 적힌 단원명', std: ['6국03-01'] },
  { grade: '5-1', no: 3, name: '교과서에 적힌 단원명', std: ['6국03-02', '6국02-04'] },
  // ...
];
```

| 필드 | 설명 |
|---|---|
| `grade` | `'5-1'` `'5-2'` `'6-1'` `'6-2'` 중 하나 |
| `no` | 단원 번호 (숫자) |
| `name` | 교과서에 **인쇄된 그대로** |
| `std` | 그 단원의 성취기준 코드 배열 — **교사용 지도서 앞부분**에 단원별로 명시되어 있습니다 |

쓸 수 있는 성취기준 코드는 같은 파일의 `WG_STD` 표(16개)에 있습니다. 필요한 코드가 없으면 `WG_STD` 에 한 줄 추가하면 됩니다.

### 방법 B — 앱 안에서 입력 (기기별 저장)

홈 화면 → `📖 이번 주 단원 고르기` → `✏️ 단원 직접 만들기`

학년·단원 번호·단원명을 넣고 성취기준을 체크하면 저장됩니다.
**단, 이 기기의 브라우저에만 저장**되므로 교실 기기마다 반복해야 합니다. 학급 전체에 쓰려면 방법 A 를 권합니다.

---

## 5. 배포 후 확인 체크리스트

브라우저 개발자도구 콘솔을 열고 확인하세요.

### 5-1. 필수 (안 되면 롤백)

- [ ] 콘솔에 `ReferenceError` 가 **없다** → 분할본이 다 올라갔다는 뜻
- [ ] 우하단 🎮 버튼이 뜨고 눌러서 게임 센터가 열린다
- [ ] 게임 하나(예: 맞춤법 몬스터)를 끝까지 플레이할 수 있다
- [ ] 그림일기를 쓰고 저장 → AI 채점이 정상 동작한다
- [ ] 세계관 프롤로그 / 성장 일지가 정상 표시된다

### 5-2. 신규 기능

- [ ] 홈 화면에 `📖 이번 주 단원 고르기` 칩이 보인다
- [ ] 영역(예: 쓰기)을 고르면 활동 목록이 좁혀진다
- [ ] 단원 선택 후 일기 화면 상단에 안내 배너가 뜬다
- [ ] 단원 선택 상태에서 미션 뽑기 → 그 영역 미션만 나온다
- [ ] 일기 화면에 `🕸️ 생각그물 30초` 배너가 보인다
- [ ] 계획을 짜면 가지 칩이 뜨고, 본문에 쓰면 ✅ 로 바뀐다
- [ ] 홈 화면에 `💾 백업 안 함` 칩(빨간색)이 보인다
- [ ] 일기를 5편 이상 쓰고 저장하면 백업 권유 모달이 뜬다

### 5-3. Fold 8 실기 확인 (특히 주의)

새 UI 3개가 추가되었습니다. **가로모드에서 겹침을 반드시 확인**하세요.

| 위치 | 요소 | 겹칠 수 있는 것 |
|---|---|---|
| 홈 화면 상단 | 📖 단원 칩 | 앱 카드 4개 그리드 |
| 홈 화면 하단 | 💾 백업 칩 | 잉크·스트릭·명예의전당·상점 (이미 한 줄에 4개) |
| 일기 화면 | 🕸️ 계획 배너 | 미션 상자 · 오감 빙고 · 게임 배너 |

특히 **홈 화면 하단 버튼 줄**은 이미 요소가 많아서 백업 칩 추가로 줄바꿈이 일어날 가능성이 높습니다. 답답해 보이면 `16-backup-nudge.js` 의 `wgInjectBkChip()` 에서 붙이는 위치를 바꾸거나, 아래 CSS 로 칩을 작게 만들 수 있습니다.

```css
.wg-bk-chip { padding: 4px 10px; font-size: 12px; }
```

---

## 6. 설계 의도 — 나중에 고칠 때 참고

### 왜 AI 진단을 안 썼나 (④ 약점 추천)

약점 판정에 AI 채점(`richness`)을 쓰지 않고 **기존 오감 빙고 정규식 9종을 재사용**했습니다.

- AI 채점은 호출마다 결과가 흔들릴 수 있어 **진단 근거로 부적절**
- 정규식은 비용 0 · 결정적 · 재현 가능
- 이미 검증된 코드를 재사용하므로 새 오류 가능성이 낮음

측정하는 것은 **실력이 아니라 최근 사용 빈도**입니다. "요즘 비유를 안 썼다"는 사실이지 "비유를 못한다"가 아닙니다. 그래서 학생에게 점수·순위로 노출하지 않고 **추천 게임 1개 + 이유 한 줄**로만 보여 줍니다.

과잉 진단 방지 장치:
- 표본 3편 미만 → 추천 안 함 (기존 랜덤으로 복귀)
- 최저 범주도 60% 이상 사용 → "약점 없음" 처리
- 동점이면 날짜 시드로 흩어서 같은 범주만 반복 추천되지 않게

### 왜 계획하기를 강제하지 않았나 (③)

계획 단계를 의무화하면 쓰기 자체를 회피하게 만들 수 있다는 우려 때문입니다. 항상 건너뛸 수 있고, 계획과 결과가 달라도 **채점하지 않습니다.**

빠진 가지가 있으면 "틀렸다"가 아니라 **"빼기로 한 건가요, 깜빡한 건가요?"** 로 묻습니다. 쓰다가 계획이 바뀌는 것은 쓰기 과정의 회귀성이라는 정상 동작이기 때문입니다.

### 백업 권유의 절제 (②)

- 하루 최대 1회
- 일기 저장 후 4.2초 뒤 (계획 대조 모달과 겹치지 않도록)
- 강제 없음 — "나중에 할게" 가 항상 있음

`navigator.storage.persisted()` 로 휘발성 저장소를 추정하지만, 이는 **"지워진다" 확정이 아니라 "지워질 수 있다" 추정**입니다. 브라우저마다 정책이 달라 오탐·미탐이 모두 가능하므로 안내 문구도 단정하지 않게 썼습니다.

---

## 7. 알려진 제약 · 다음에 볼 것

### 성능

14 · 15 · 16 번이 각각 3~5초 간격 `setInterval` 폴링을 씁니다 (화면 전환 감지용). 개별 부담은 작지만, 나중에 화면 전환 이벤트를 하나로 모아 이벤트 기반으로 바꾸면 더 깔끔합니다.

### 데이터 저장 위치

| 키 | 내용 | 범위 |
|---|---|---|
| `mdj_wg_expr_<닉네임>` | 표현 프로필 (최근 10편) | 학생별 |
| `mdj_units` | 교사가 만든 단원 | **기기 공통** |
| `mdj_cur_unit` | 현재 선택 단원 | **기기 공통** |
| `mdj_plan` | 생각그물 계획 | 기기 공통 · 하루치 |
| `mdj_backup_state` | 백업 이력 | 기기 공통 |

⚠️ 단원 선택은 **기기 단위**입니다. 한 기기를 여러 학생이 돌려쓰는 구조와 잘 맞지만, 학생마다 다른 단원을 하게 하려면 키에 닉네임을 붙이도록 바꿔야 합니다.

### 아직 안 한 것 (이전 논의의 6~14번)

- 비계 소거 (누적 기록에 따라 힌트 줄이기)
- 점수·코멘트 분리 (코멘트 먼저 → 점수는 눌러서 보기)
- 맞춤법 오답 노트 · 간격 반복 재출제
- 자기평가 사전 목표 설정 (SRL 순환 닫기)
- 세계관 5장 이후 콘텐츠
- 접근성 (`aria-*` 속성이 여전히 0개)
- 개인정보 고지 문구
